== Csabuilder

git clone git://github.com:activefx/csabuilder.git
rename csabuilder <new_application_name>
git remote rename origin csabuilder
bundle install
rake development:install
git commit -a -m 'installed csabuilder'



# Build PhantomJS from source
  sudo apt-get install libqt4-dev libqtwebkit-dev qt4-qmake
  git clone git://github.com/ariya/phantomjs.git && cd phantomjs
  git checkout 1.3
  qmake-qt4 && make

